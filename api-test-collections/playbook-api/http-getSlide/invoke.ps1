if (-not $env:PLAYBOOK_ID -or -not $env:SLIDE_ID) { throw "Set PLAYBOOK_ID and SLIDE_ID to existing IDs." }
& (Join-Path $PSScriptRoot "..\Invoke-Api.ps1") -Method Get -Path "/playbooks/$env:PLAYBOOK_ID/slides/$env:SLIDE_ID"