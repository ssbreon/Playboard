if (-not $env:GAME_PLAN_ID) { throw "Set GAME_PLAN_ID to an existing game plan ID." }
& (Join-Path $PSScriptRoot "..\Invoke-Api.ps1") -Method Post -Path "/game-plans/$env:GAME_PLAN_ID/scout-plays" -BodyPath (Join-Path $PSScriptRoot "sample-data.json")